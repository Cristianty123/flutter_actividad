import 'package:flutter/material.dart';

const kAvatarBgDuration = Duration(milliseconds: 300);
const kAvatarFgDelay = Duration(milliseconds: 160);
const kAvatarFgDuration = Duration(milliseconds: 150);
const kMessageScaleDuration = Duration(milliseconds: 180);
const kMessageTextDelay = Duration(milliseconds: 100);
const kMessageTextDuration = Duration(milliseconds: 130);
const kLineDuration = Duration(milliseconds: 180);

const kEaseOutBack = Curves.easeOutBack;